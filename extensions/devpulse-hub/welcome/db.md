# Connect Your Databases

DevForge ships with first-class support for the database engines DevPulse uses every day.

## Supported

- **SurrealDB** — multi-model graph + document
- **libSQL** — embedded and remote SQLite
- **DuckDB** — in-process analytics

Open the **DevPulse DB Bridge** view from the Command Palette to connect.
